import styles from '../styles/Error.module.css';

const ErrorScreen = () => {
  return (
    <main className={styles.container}>
      Lo sentimos, <br />
      ocurrió un error
    </main>
  );
};

export default ErrorScreen;
